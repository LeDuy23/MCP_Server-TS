{
  "servers": {
    "test-mcp-video-server": {
      "type": "stdio",
      "command": "npm",
      "args": ["run" , "server:dev"],
      "cwd": "${workspaceFolder}",
      "dev": { 
        "watch": "build/**/*.js",
        "debug": {
          "type": "node"
        }
      }
    }
  }
}